framework:
    cache:
        # Redis-backed cache for rate limiting and application cache
        app: cache.adapter.redis
        default_redis_provider: '%env(REDIS_URL)%'

when@test:
    framework:
        cache:
            app: cache.adapter.filesystem

when@e2e:
    framework:
        cache:
            app: cache.adapter.filesystem
